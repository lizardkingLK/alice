# SonarCloud — monorepo analysis configuration
# https://docs.sonarsource.com/sonarqube-cloud

sonar.organization=lizardkinglk
sonar.projectKey=lizardkingLK_alice

# Scope
sonar.sources=apps,packages
sonar.tests=apps,packages
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Noise reduction (generated, build output, vendor)
sonar.exclusions=\
**/node_modules/**,\
**/.next/**,\
**/dist/**,\
**/coverage/**,\
**/generated/**,\
**/prisma/migrations/**,\
**/.turbo/**,\
**/pnpm-lock.yaml

# Coverage disabled until Vitest LCOV is wired in CI
sonar.coverage.exclusions=**/*

# TypeScript / JavaScript monorepo
sonar.javascript.node.maxspace=4096

# Static catalog / mock fixture blobs — not logic duplication
# (charts-sample.data.ts still hosts filter helpers; CPD ignores this file so
# repeated fixture encodings cannot fail the new-code duplication gate.)
sonar.cpd.exclusions=\
**/settings-integration-catalog.data.json,\
**/charts-sample.data.ts
