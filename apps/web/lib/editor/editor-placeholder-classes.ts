/**
 * TipTap Placeholder `emptyEditorClass` — paints the hint only when the whole
 * doc is empty (not on trailing empty paragraphs after Enter / lists).
 */
export const EDITOR_EMPTY_PLACEHOLDER_CLASS =
  'is-editor-empty before:pointer-events-none before:float-left before:h-0 before:text-muted-foreground before:content-[attr(data-placeholder)]';
