import {
  REGISTRY_PAGES,
  RegistryLoadingPage,
} from '@/components/registry-page-shell';

export default function ViewsLoadingPage() {
  return <RegistryLoadingPage meta={REGISTRY_PAGES.views} />;
}
