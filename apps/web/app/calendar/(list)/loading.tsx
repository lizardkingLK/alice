import {
  REGISTRY_PAGES,
  RegistryLoadingPage,
} from '@/components/registry-page-shell';

export default function CalendarLoading() {
  return <RegistryLoadingPage meta={REGISTRY_PAGES.calendar} />;
}
